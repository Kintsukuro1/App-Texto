export const vocabulary = {
  page: 'nota',        // en vez de "página"
  workspace: 'espacio', // en vez de "workspace"
  block: 'bloque',      // se queda igual, es suficientemente neutro
  member: 'persona',    // en vez de "miembro del equipo"
} as const;
